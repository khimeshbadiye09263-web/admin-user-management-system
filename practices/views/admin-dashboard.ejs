<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 min-h-screen">

    <div class="container mx-auto px-4 py-8">
        <!-- Welcome Section -->
        <div class="bg-white bg-opacity-10 backdrop-blur-lg rounded-2xl shadow-2xl p-8 mb-8">
            <h1 class="text-5xl font-bold text-white mb-4">
                👋 Welcome, <%= admin.name %>!
            </h1>
            <p class="text-gray-300 text-lg">
                You are logged in as: <span class="text-yellow-400 font-semibold">
                    <%= admin.email %>
                </span>
            </p>
            <p class="text-gray-400 mt-2">
                Role: <span
                    class="px-3 py-1 bg-gradient-to-r from-yellow-400 to-orange-500 text-white text-sm font-bold rounded-full">
                    👑 <%= admin.role.toUpperCase() %>
                </span>
            </p>
        </div>

        <!-- Statistics Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <!-- Total Users Card -->
            <div
                class="bg-gradient-to-br from-blue-500 to-blue-700 rounded-2xl shadow-2xl p-6 transform hover:scale-105 transition duration-300">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-blue-100 text-sm font-semibold uppercase tracking-wide">Total Users</p>
                        <p class="text-white text-4xl font-bold mt-2">
                            <%= stats.totalUsers %>
                        </p>
                    </div>
                    <div class="text-6xl opacity-20">👥</div>
                </div>
            </div>

            <!-- Admins Card -->
            <div
                class="bg-gradient-to-br from-yellow-500 to-orange-600 rounded-2xl shadow-2xl p-6 transform hover:scale-105 transition duration-300">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-yellow-100 text-sm font-semibold uppercase tracking-wide">Admins</p>
                        <p class="text-white text-4xl font-bold mt-2">
                            <%= stats.totalAdmins %>
                        </p>
                    </div>
                    <div class="text-6xl opacity-20">👑</div>
                </div>
            </div>

            <!-- Regular Users Card -->
            <div
                class="bg-gradient-to-br from-purple-500 to-indigo-700 rounded-2xl shadow-2xl p-6 transform hover:scale-105 transition duration-300">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-purple-100 text-sm font-semibold uppercase tracking-wide">Regular Users</p>
                        <p class="text-white text-4xl font-bold mt-2">
                            <%= stats.regularUsers %>
                        </p>
                    </div>
                    <div class="text-6xl opacity-20">👤</div>
                </div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="bg-white bg-opacity-10 backdrop-blur-lg rounded-2xl shadow-2xl p-8">
            <h2 class="text-3xl font-bold text-white mb-6">🎯 Quick Actions</h2>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <a href="/admin/users-page"
                    class="group bg-gradient-to-r from-indigo-600 to-purple-600 p-6 rounded-xl hover:from-indigo-700 hover:to-purple-700 transform hover:scale-105 transition duration-200 shadow-lg">
                    <div class="flex items-center">
                        <span class="text-4xl mr-4">👥</span>
                        <div>
                            <h3 class="text-xl font-bold text-white">Manage Users</h3>
                            <p class="text-purple-100 text-sm">View, edit, and delete users</p>
                        </div>
                    </div>
                </a>

                <a href="/admin/users"
                    class="group bg-gradient-to-r from-blue-600 to-cyan-600 p-6 rounded-xl hover:from-blue-700 hover:to-cyan-700 transform hover:scale-105 transition duration-200 shadow-lg">
                    <div class="flex items-center">
                        <span class="text-4xl mr-4">📊</span>
                        <div>
                            <h3 class="text-xl font-bold text-white">View API Data</h3>
                            <p class="text-cyan-100 text-sm">Get users in JSON format</p>
                        </div>
                    </div>
                </a>
            </div>

            <a href="/logout"
                class="inline-block mt-6 px-8 py-3 bg-red-500 text-white font-semibold rounded-lg hover:bg-red-600 transform hover:scale-105 transition duration-200 shadow-lg">
                🚪 Logout
            </a>
        </div>

        <!-- Pro Tips Section -->
        <div class="mt-8 bg-white bg-opacity-10 backdrop-blur-lg rounded-2xl shadow-2xl p-6">
            <h3 class="text-2xl font-bold text-white mb-4">💡 Pro Tips</h3>
            <ul class="space-y-2 text-gray-300">
                <li class="flex items-start">
                    <span class="text-green-400 mr-2">✓</span>
                    <span>Use the search feature to quickly find users by name or email</span>
                </li>
                <li class="flex items-start">
                    <span class="text-green-400 mr-2">✓</span>
                    <span>Click on any user to edit their details or change their role</span>
                </li>
                <li class="flex items-start">
                    <span class="text-green-400 mr-2">✓</span>
                    <span>You cannot delete your own admin account for security reasons</span>
                </li>
                <li class="flex items-start">
                    <span class="text-green-400 mr-2">✓</span>
                    <span>Use <code class="bg-gray-800 px-2 py-1 rounded text-sm">POST /admin/create-admin</code> API to
                        create new admins</span>
                </li>
            </ul>
        </div>
    </div>

</body>

</html>